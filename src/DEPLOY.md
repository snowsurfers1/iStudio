# 🚀 Деплой на GitHub Pages

## Шаг 1: Создайте репозиторий на GitHub

1. Перейдите на [GitHub](https://github.com) и создайте новый репозиторий
2. Назовите его, например: `istudio-website`
3. **НЕ** добавляйте README, .gitignore или лицензию (они уже есть в проекте)

## Шаг 2: Загрузите код в репозиторий

Выполните следующие команды в терминале (в папке вашего проекта):

```bash
git init
git add .
git commit -m "Initial commit: iStudio website"
git branch -M main
git remote add origin https://github.com/ВАШ-USERNAME/istudio-website.git
git push -u origin main
```

**Замените** `ВАШ-USERNAME` на ваше имя пользователя GitHub и `istudio-website` на название вашего репозитория.

## Шаг 3: Настройте GitHub Pages

1. Перейдите в настройки вашего репозитория на GitHub
2. В боковом меню выберите **Pages**
3. В разделе **Source** выберите **GitHub Actions**

## Шаг 4: Настройте базовый путь (если нужно)

### Если ваш репозиторий называется `ваш-username.github.io`:
Ничего не нужно менять! Сайт будет доступен по адресу: `https://ваш-username.github.io`

### Если у вас другое название репозитория (например, `istudio-website`):
1. Откройте файл `/vite.config.ts`
2. Измените строку `base: '/'` на `base: '/istudio-website/'` (название вашего репозитория)
3. Сохраните изменения
4. Закоммитьте и запушьте:
```bash
git add vite.config.ts
git commit -m "Update base path for GitHub Pages"
git push
```

Сайт будет доступен по адресу: `https://ваш-username.github.io/istudio-website`

## Шаг 5: Дождитесь деплоя

1. Перейдите во вкладку **Actions** в вашем репозитории
2. Вы увидите процесс деплоя (обычно занимает 1-3 минуты)
3. Когда появится зеленая галочка ✅ - сайт опубликован!
4. Перейдите на ваш сайт по указанному URL

## 🎉 Готово!

Теперь каждый раз, когда вы пушите изменения в ветку `main`, сайт будет автоматически обновляться!

## 📝 Обновление сайта

Чтобы обновить сайт:

```bash
git add .
git commit -m "Описание изменений"
git push
```

Автоматический деплой запустится сразу после push!

## ⚠️ Важно

- Убедитесь, что в настройках репозитория включены **Actions** (Settings → Actions → General → Allow all actions)
- Первый деплой может занять до 10 минут
- Изменения на сайте могут отображаться с задержкой до 5 минут из-за кеша браузера
